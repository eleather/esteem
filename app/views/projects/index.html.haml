%h1 Listing projects

%table
  %tr
    %th Name
    %th Slug
    %th Organization
    %th
    %th
    %th

  - @projects.each do |project|
    %tr
      %td= project.name
      %td= project.slug
      %td= link_to project.organization.name, project.organziation
      %td= link_to 'Show', project
      %td= link_to 'Edit', edit_project_path(project)
      %td= link_to 'Destroy', project, :confirm => 'Are you sure?', :method => :delete
